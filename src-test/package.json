{
    "name": "dm-test",
    "version": "3.0.0",
    "private": true,
    "dependencies": {
        "@emotion/cache": "^11.14.0",
        "@emotion/react": "^11.14.0",
        "@emotion/styled": "^11.14.1",
        "@vitejs/plugin-react": "^6.1.1",
        "react": "^19.3.0",
        "react-dom": "^19.3.0",
        "react-icons": "^5.7.0"
    },
    "homepage": ".",
    "proxy": "http://localhost:8081",
    "scripts": {
        "start": "vite dev --config vite.config.mts --host",
        "build": "vite build --config vite.config.mts",
        "check-ts": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p tsconfig.node.json",
        "lint": "eslint -c eslint.config.mjs"
    },
    "eslintConfig": {
        "extends": "react-app"
    },
    "browserslist": [
        ">0.2%",
        "not dead",
        "not ie <= 11",
        "not op_mini all"
    ],
    "devDependencies": {
        "@types/node": "^26.6.1"
    }
}
